import React from 'react';
import TestCatalog from './TestCatalog';

const Test = (props) => {
    return (
        <>
            <h1> Object id: {props.id}</h1>
        </>
    )
}

export default Test